import React from 'react';
import { Carousel } from 'bootstrap';

const HomeCarousel = () => {
    const createCarousel = new Carousel('#myCarousel');
  return (
    <div id='myCarousel'>
        a
    </div>
  );
};

export default HomeCarousel;